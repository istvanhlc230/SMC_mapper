# RISK — DEPRECATED FILENAME

The canonical risk specification is now maintained in:

```text
.agents/skills/smc/05_risk.md
```

This file is retained only as a compatibility pointer so the legacy filename does not contain a competing or stale risk specification.

Do not add canonical risk rules here. Update `05_risk.md` instead.
